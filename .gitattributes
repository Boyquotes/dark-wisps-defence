*.png filter=lfs diff=lfs merge=lfs -text
*.dwd filter=lfs diff=lfs merge=lfs -text

# Force LF line endings for SQL migration files to prevent checksum mismatches
# across platforms (Windows CRLF vs Unix LF). Migration runners calculate checksums
# based on file content, so consistent line endings are critical.
*.sql text eol=lf
